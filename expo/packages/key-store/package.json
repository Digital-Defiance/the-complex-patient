{
  "name": "@complex-patient/key-store",
  "version": "0.0.1",
  "private": true,
  "description": "Session Key Store: platform-specific KEK storage + shared idle auto-lock (Requirement 3)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@complex-patient/crypto-engine": "workspace:*"
  }
}
