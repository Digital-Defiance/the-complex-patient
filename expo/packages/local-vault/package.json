{
  "name": "@complex-patient/local-vault",
  "version": "0.0.1",
  "private": true,
  "description": "Encrypted persistence abstraction (sqlite / MMKV)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@complex-patient/domain": "workspace:*"
  }
}
