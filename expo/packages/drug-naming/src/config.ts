/** Master switch — set false to disable all drug-naming UI without a release. */
export const DRUG_NAMING_ASSIST_ENABLED = true;

/** Minimum match score before suggesting a concept to the user. */
export const RX_MATCH_SUGGEST_THRESHOLD = 0.55;

/** Minimum score to pre-select a match for confirmation. */
export const RX_MATCH_CONFIRM_THRESHOLD = 0.75;
