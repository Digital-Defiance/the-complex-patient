{
  "name": "@complex-patient/weather",
  "version": "0.0.1",
  "private": true,
  "description": "Historical weather enrichment via Open-Meteo (location ports + local cache)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@complex-patient/domain": "workspace:*"
  }
}
